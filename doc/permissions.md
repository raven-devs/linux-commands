# Permissions

```bash
# print a current user name
whoami

# print current user id and a current user group id
echo $UID
echo $GID

# grant read-write-execute permissions to a file or a dir recursively
chmod -R +rwx $file_path
```
